using System;
using System.Collections.Generic;
using Bukovac.Graphics;

partial class Program
{
    private static void AddAllSamples(
        List<(string Name, Action<Canvas, int, int> Render)> samples,
        FontSpec ui,
        FontSpec title,
        string uiFamily,
        string monoFamily,
        string emojiFamily)
    {
        AddSample_01(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_02(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_03(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_04(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_05(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_06(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_07(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_08(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_09(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_10(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_11(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_12(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_13(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_14(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_15(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_16(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_17(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_18(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_19(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_20(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_21(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_22(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_23(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_24(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_25(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_26(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_27(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_28(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_29(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_30(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_31(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_32(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_33(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_34(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_35(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_36(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_37(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_38(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_39(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_40(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_41(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_42(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_43(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_44(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_45(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_46(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_47(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_48(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_49(samples, ui, title, uiFamily, monoFamily, emojiFamily);
        AddSample_50(samples, ui, title, uiFamily, monoFamily, emojiFamily);
    }
}
